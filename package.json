{
  "name": "tgma-hackathon",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/kennygomel/tgma-hackathon.git",
  "license": "MIT",
  "scripts": {
    "start:dev": "docker compose up -f docker-compose-core.yml -f docker-compose-dev.yml -d",
    "start:prod": "docker compose up -f docker-compose-core.yml -f docker-compose-prod.yml -d",
    "stop": "docker compose down",
    "rebuild:api": "docker volume rm tgma-hackathon_api-node-modules-data -f && docker compose build --no-cache api",
    "rebuild:ui": "docker volume rm tgma-hackathon_ui-node-modules-data -f && docker compose build --no-cache ui"
  },
  "dependencies": {
    "api": "file:api",
    "ui": "file:ui"
  }
}
